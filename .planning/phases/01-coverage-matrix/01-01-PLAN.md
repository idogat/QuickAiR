---
phase: 01-coverage-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/COVERAGE-MATRIX.md
autonomous: true
requirements:
  - COV-01
  - COV-02
  - COV-03
  - COV-04

must_haves:
  truths:
    - "An analyst can look up any artifact type (Processes, Network TCP, Network DNS, DLLs, Users) and see which data fields are collected at each PS version tier"
    - "Fallback chains are documented per-collector with exact source field string values from the codebase"
    - "Coverage gaps are explicitly listed per-collector and toolkit-wide — DLLs protected process exclusion, Users replication lag, netstat timestamp absence, toolkit scope boundaries"
    - "Fidelity notes name exact fields that are null or degraded at each tier, not vague 'degraded' labels"
    - "Document renders cleanly as GitHub-flavored Markdown with stable anchor slugs for Phase 2/3 cross-references"
  artifacts:
    - path: "docs/COVERAGE-MATRIX.md"
      provides: "Complete artifact coverage matrix reference document"
      contains: "# DFIR Artifact Coverage Matrix"
      min_lines: 200
  key_links:
    - from: "docs/COVERAGE-MATRIX.md"
      to: "Modules/Collectors/Processes.psm1"
      via: "Fallback chain and field fidelity data derived from source"
      pattern: "cim.*wmi.*dotnet_fallback"
    - from: "docs/COVERAGE-MATRIX.md"
      to: "Modules/Collectors/Network.psm1"
      via: "TCP and DNS independent fallback chains derived from source"
      pattern: "netstat_fallback.*ipconfig_fallback"
    - from: "docs/COVERAGE-MATRIX.md"
      to: "Modules/Collectors/DLLs.psm1"
      via: "DLLs .NET vs WMI enumeration and protected process list from source"
      pattern: "lsass.*csrss.*smss.*wininit.*System.*Idle"
    - from: "docs/COVERAGE-MATRIX.md"
      to: "Modules/Collectors/Users.psm1"
      via: "Users WMI+Registry collection and DC detection from source"
      pattern: "WMI\\+Get-ADUser.*WMI\\+ADSI"
---

<objective>
Write the complete docs/COVERAGE-MATRIX.md document — an artifact x data source grid with fallback paths, field fidelity tables, and explicit coverage gaps for all five collectors.

Purpose: Analysts and downstream document authors (Phase 2 Runbook, Phase 3 Interpretation Guide) need a verified, accurate reference showing exactly what each collector gathers, at which fallback tier, and where coverage gaps exist. This is the ground-truth anchor for the entire documentation suite.

Output: `docs/COVERAGE-MATRIX.md` — a single self-contained markdown document with stable heading anchors.
</objective>

<execution_context>
@C:/Windows/System32/.claude/get-shit-done/workflows/execute-plan.md
@C:/Windows/System32/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-coverage-matrix/01-CONTEXT.md
@.planning/phases/01-coverage-matrix/01-RESEARCH.md
@.planning/phases/01-coverage-matrix/01-UI-SPEC.md

Source files to read for verification (per STATE.md decision):
@Modules/Collectors/Processes.psm1
@Modules/Collectors/Network.psm1
@Modules/Collectors/DLLs.psm1
@Modules/Collectors/Users.psm1
@Modules/Core/Connection.psm1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs directory and write document skeleton with Legend, Tier Model, and Capability Probe sections</name>
  <files>docs/COVERAGE-MATRIX.md</files>
  <read_first>
    - C:/DFIRLab/repo/Modules/Core/Connection.psm1 (source of truth for Get-TargetCaps capability probe — tier boundary logic, HasCIM/HasNetTCPIP/HasDNSClient detection)
    - C:/DFIRLab/repo/.planning/phases/01-coverage-matrix/01-RESEARCH.md (pre-verified data for all sections)
    - C:/DFIRLab/repo/.planning/phases/01-coverage-matrix/01-UI-SPEC.md (document structure contract, heading text, notation standards, copywriting contract)
    - C:/DFIRLab/repo/.planning/phases/01-coverage-matrix/01-CONTEXT.md (locked decisions D-01, D-02)
  </read_first>
  <action>
Create the `docs/` directory. Then write `docs/COVERAGE-MATRIX.md` with the following sections only (collector sections come in Task 2 and Task 3):

**H1 title (exact text per UI-SPEC):** `# DFIR Artifact Coverage Matrix`

**## Legend section:**
- Open with: `Use this key to read the tables throughout this document.`
- Define the three tier column header labels (exact text per UI-SPEC Notation Contract):
  - `PS 5.1 / Full CIM` — PSVersion >= 3 AND HasNetTCPIP=true AND HasDNSClient=true
  - `PS 3+ / Partial CIM` — PSVersion >= 3 AND (HasNetTCPIP=false OR HasDNSClient=false)
  - `PS 2.0 / Legacy` — PSVersion < 3
- Define cell state notation (per UI-SPEC Notation Contract): `populated`, `null (all tiers)`, `null`, `derived — {reason}`, `PS2_SIGNED / PS2_UNSUPPORTED`, `string (regex parse)`, backtick code spans for method names and source values
- Define coverage gap notation format: `- **{Gap name}**: {one-sentence description}`

**## Tier Model section:**
- Open with (per UI-SPEC copywriting): `The collection method and data fidelity depend on the PowerShell version and available CIM namespaces on the target. The capability probe in Connection.psm1 detects these at runtime.`
- Include tier definition table (per D-01, verified from Connection.psm1 Get-TargetCaps):

| Tier | Condition | CIM Classes Available |
|------|-----------|----------------------|
| PS 5.1 / Full CIM | PSVersion >= 3, HasNetTCPIP=true, HasDNSClient=true | `Win32_Process`, `MSFT_NetTCPConnection`, `MSFT_DNSClientCache` |
| PS 3+ / Partial CIM | PSVersion >= 3, HasNetTCPIP=false or HasDNSClient=false | `Win32_Process` (CIM), but netstat/ipconfig for Network/DNS |
| PS 2.0 / Legacy | PSVersion < 3 | No CIM; WMI `Win32_Process`, `cmd netstat`, `cmd ipconfig` |

- Add a note: `HasNetTCPIP` and `HasDNSClient` are probed independently. A PS 5.1 target can have `HasNetTCPIP=false` if the NetTCPIP module is absent (e.g., Server Core minimal install). Each Network sub-collector (TCP and DNS) branches independently on its own flag.

**## Capability Probe section:**
- Open with (per UI-SPEC copywriting): `The \`Get-TargetCaps\` function in \`Modules/Core/Connection.psm1\` runs on the remote session before any collector executes. It returns the following capability flags that each collector uses to select its data path.`
- Include capability probe output table (verified from Connection.psm1):

| Flag | Type | How Detected |
|------|------|-------------|
| `PSVersion` | int | `$PSVersionTable.PSVersion.Major` |
| `HasCIM` | bool | PSVersion >= 3 |
| `HasNetTCPIP` | bool | `Get-CimInstance MSFT_NetTCPConnection` accessible in `ROOT/StandardCimv2` |
| `HasDNSClient` | bool | `Get-CimInstance MSFT_DNSClientCache` accessible in `ROOT/StandardCimv2` |
| `IsServerCore` | bool | Registry or feature detection |
| `TimezoneOffsetMinutes` | int | Target timezone offset for UTC conversion |

Leave placeholder comments at the end of the file where Task 2 and Task 3 will append collector sections. Use the format:
```
<!-- Collector sections follow -->
```

Per D-02, all collector sections will use this column layout for their summary row:
`| Artifact | Data Fields | PS 5.1 (Full CIM) | PS 3+ (Partial CIM) | PS 2.0 (Legacy) | Fidelity Notes | Coverage Gaps |`

All PowerShell class names, method names, CIM class names, field names, and source string values MUST appear in backtick code spans per UI-SPEC Typography rules.
All file paths and parameter names MUST appear in backtick code spans.
Two blank lines before each H2 heading per UI-SPEC Spacing Scale.
  </action>
  <verify>
    <automated>pwsh -Command "Test-Path C:/DFIRLab/repo/docs/COVERAGE-MATRIX.md"</automated>
  </verify>
  <acceptance_criteria>
    - docs/COVERAGE-MATRIX.md exists and is non-empty
    - File starts with `# DFIR Artifact Coverage Matrix`
    - File contains `## Legend` heading
    - File contains `## Tier Model` heading
    - File contains `## Capability Probe` heading
    - Legend section contains all three tier label strings: `PS 5.1 / Full CIM`, `PS 3+ / Partial CIM`, `PS 2.0 / Legacy`
    - Tier Model section contains `HasNetTCPIP` and `HasDNSClient` as independent probe flags
    - Capability Probe section contains `Get-TargetCaps` in backtick code span
    - Capability Probe section contains all six flags: PSVersion, HasCIM, HasNetTCPIP, HasDNSClient, IsServerCore, TimezoneOffsetMinutes
    - No heading text deviates from UI-SPEC anchor stability contract
  </acceptance_criteria>
  <done>docs/COVERAGE-MATRIX.md exists with H1 title, Legend, Tier Model, and Capability Probe sections fully written. All notation standards and copywriting match the UI-SPEC contract. File is ready for Task 2 to append collector sections.</done>
</task>

<task type="auto">
  <name>Task 2: Write Processes and Network (TCP + DNS) collector sections</name>
  <files>docs/COVERAGE-MATRIX.md</files>
  <read_first>
    - C:/DFIRLab/repo/docs/COVERAGE-MATRIX.md (current state from Task 1)
    - C:/DFIRLab/repo/Modules/Collectors/Processes.psm1 (source of truth — CIM vs WMI branching, .NET fallback, Signature per tier)
    - C:/DFIRLab/repo/Modules/Collectors/Network.psm1 (source of truth — TCP CIM/netstat, DNS CIM/ipconfig independent chains, InterfaceAlias derivation)
    - C:/DFIRLab/repo/.planning/phases/01-coverage-matrix/01-RESEARCH.md (pre-verified field fidelity tables and fallback chains)
    - C:/DFIRLab/repo/.planning/phases/01-coverage-matrix/01-UI-SPEC.md (table column layout contract, notation standards)
    - C:/DFIRLab/repo/.planning/phases/01-coverage-matrix/01-CONTEXT.md (locked decisions D-03, D-04, D-07 through D-11, D-14, D-15)
  </read_first>
  <action>
Append three collector sections to docs/COVERAGE-MATRIX.md (replacing the placeholder comment). Each section follows the per-collector sub-table pattern per D-03 (each collector documented individually — NOT uniform).

**## Processes section:**

Per UI-SPEC copywriting template: `Processes collects running process details from the target. The collection method and available data fields vary by tier.`

Fallback chain table (per D-07, verified from Processes.psm1):

| Tier | Condition | Collection Method | `source` value |
|------|-----------|-------------------|----------------|
| PS 3+ | HasCIM=true | `Get-CimInstance Win32_Process` | `cim` |
| PS 2.0 | PSVersion < 3 | `ManagementObjectSearcher Win32_Process` (WMI) | `wmi` |
| Any (supplement) | PIDs in .NET not in WMI/CIM | `[System.Diagnostics.Process]::GetProcesses()` | `dotnet_fallback` |

Add the dotnet_fallback supplement note (per UI-SPEC copywriting, exact text):
`Supplement at all tiers: processes absent from WMI/CIM are added via .NET System.Diagnostics.Process.GetProcesses() with source = dotnet_fallback. These entries have null values for: ParentProcessId, CommandLine, SessionId, HandleCount.`

Note combined source strings: `cim+dotnet_fallback` or `wmi+dotnet_fallback`.

Field fidelity table (per RESEARCH.md verified data, per D-08, D-09):

| Field | PS 5.1 / Full CIM | PS 3+ / Partial CIM | PS 2.0 / Legacy | dotnet_fallback |
|-------|-------------------|---------------------|-----------------|-----------------|
| `ProcessId` | populated | populated | populated | populated |
| `ParentProcessId` | populated | populated | populated | **null** |
| `Name` | populated | populated | populated | populated (`ProcessName`) |
| `CommandLine` | populated | populated | populated | **null** |
| `ExecutablePath` | populated | populated | populated | populated (`MainModule.FileName`) |
| `CreationDateUTC` | populated (UTC) | populated (UTC) | populated (WMI DMTF converted) | populated (`StartTime` UTC) |
| `WorkingSetSize` | populated | populated | populated | populated (`WorkingSet64`) |
| `VirtualSize` | populated | populated | populated | populated (`VirtualMemorySize64`) |
| `SessionId` | populated | populated | populated | **null** |
| `HandleCount` | populated | populated | populated | **null** |
| `SHA256` | populated | populated | populated | populated if path available |
| `Signature.IsValid` | populated (bool) | populated (bool) | **null** | populated (PS3+) or null (PS2) |
| `Signature.Thumbprint` | populated | populated | **null** | populated (PS3+) or null (PS2) |
| `Signature.NotAfter` | populated | populated | **null** | populated (PS3+) or null (PS2) |
| `Signature.TimeStamper` | populated | populated | **null** | populated (PS3+) or null (PS2) |
| `Signature.Status` | populated (e.g. `Valid`) | populated | `PS2_SIGNED` or `PS2_UNSUPPORTED` | populated (PS3+) |
| `Signature.SignerSubject` | populated | populated | populated (X509 Subject only) | populated (PS3+) |

Note: PS 5.1 and PS 3+ columns are identical for Processes — both use CIM Win32_Process. The distinction matters for Network (HasNetTCPIP/HasDNSClient) but not for Processes. Document this clearly.

Add PS 2.0 Signature limitation note (per UI-SPEC copywriting, exact text):
`PS 2.0 Signature uses X509Certificate.CreateFromSignedFile() — no Get-AuthenticodeSignature. Status is PS2_SIGNED or PS2_UNSUPPORTED only. IsValid, Thumbprint, NotAfter, TimeStamper are always null.`

Coverage gaps for Processes (per D-14):
- **dotnet_fallback field losses**: `ParentProcessId`=null, `CommandLine`=null, `SessionId`=null, `HandleCount`=null — process tree reconstruction impossible for these entries
- **PS 2.0 Signature limitation**: Binary signed/not-signed status only — no certificate validation detail

**## Network — TCP Connections section:**

Per UI-SPEC copywriting template: `Network — TCP Connections collects active TCP connection details from the target. The collection method and available data fields vary by tier.`

Fallback chain table (per D-04, verified from Network.psm1):

| Tier | Condition | Collection Method | `source.network` value |
|------|-----------|-------------------|------------------------|
| Full CIM | HasNetTCPIP=true | `Get-CimInstance MSFT_NetTCPConnection` (`ROOT/StandardCimv2`) | `cim` |
| PS 3+ partial | PSVersion>=3, HasNetTCPIP=false | `& netstat -ano` (PowerShell invocation) | `netstat_fallback` |
| PS 2.0 | PSVersion < 3 | `cmd /c "netstat -ano"` | `netstat_legacy` |

Field fidelity table (per RESEARCH.md verified data, per D-10, D-15):

| Field | CIM | `netstat_fallback` / `netstat_legacy` |
|-------|-----|---------------------------------------|
| `LocalAddress` | populated | populated (regex parse) |
| `LocalPort` | populated | populated (regex parse) |
| `RemoteAddress` | populated | populated (regex parse) |
| `RemotePort` | populated | populated (regex parse) |
| `OwningProcess` (PID) | populated | populated (regex parse, last column) |
| `State` | populated (converted via `ConvertFrom-TcpStateInt`) | populated (string from netstat, e.g. `ESTABLISHED`) |
| `InterfaceAlias` | CIM-native field | derived — IP-to-adapter matching via `Resolve-InterfaceAlias`; unmatched IPs get `UNKNOWN` |
| `CreationTime` | null (all tiers) — `MSFT_NetTCPConnection` does not expose this field | null (all tiers) |
| `ReverseDns` | populated (post-collection DNS lookup) | populated (same post-collection step) |
| `DnsMatch` | populated (DNS cache cross-reference) | populated (same step) |
| `IsPrivateIP` | populated | populated |

Coverage gaps for Network TCP (per D-15):
- **No connection creation timestamps**: `CreationTime` is null at ALL tiers — neither CIM nor netstat provides this field
- **InterfaceAlias less precise at netstat tiers**: Derived from IP-to-adapter matching, not CIM-native; unmatched addresses return `UNKNOWN`
- **Netstat parsing on legacy OS**: PS 2.0 targets (Server 2008 R2) may miss short-lived connections on high-churn workloads (known bug — T3 test allows 10% miss rate)

**## Network — DNS Cache section:**

Per UI-SPEC copywriting template: `Network — DNS Cache collects the DNS resolver cache from the target. The collection method and available data fields vary by tier.`

Fallback chain table (per D-04, verified from Network.psm1):

| Tier | Condition | Collection Method | `source.dns` value |
|------|-----------|-------------------|-------------------|
| Full CIM | HasDNSClient=true | `Get-CimInstance MSFT_DNSClientCache` (`ROOT/StandardCimv2`) | `cim` |
| PS 3+ partial | PSVersion>=3, HasDNSClient=false | `& ipconfig /displaydns` + regex parse | `ipconfig_fallback` |
| PS 2.0 | PSVersion < 3 | `cmd /c "ipconfig /displaydns"` + regex parse | `ipconfig_legacy` |

Field fidelity table (per RESEARCH.md verified data, per D-11):

| Field | CIM | `ipconfig_fallback` / `ipconfig_legacy` |
|-------|-----|----------------------------------------|
| `Entry` | populated | populated (regex: `Record Name`) |
| `Name` | populated | populated (same as Entry) |
| `Data` | populated | populated (IP from `Host Record`) |
| `Type` | integer (e.g. 1=A, 28=AAAA) | string (regex parse of `Record Type`) |
| `TTL` | integer | integer (regex parse of `Time To Live` line) |

Coverage gaps for Network DNS (per D-11):
- **DNS Type field format difference**: CIM returns integer type code; ipconfig path returns string representation — consumers must handle both formats
- **TTL parsing fragility**: ipconfig TTL extracted via regex `Time To Live[\s.]*:[\s]*(\d+)` — Windows ipconfig output formatting has varied across OS versions; TTL may be 0 or incorrect on untested OS versions
  </action>
  <verify>
    <automated>pwsh -Command "$f = Get-Content C:/DFIRLab/repo/docs/COVERAGE-MATRIX.md -Raw; if ($f -match '## Processes' -and $f -match '## Network .* TCP Connections' -and $f -match '## Network .* DNS Cache') { 'PASS' } else { 'FAIL' }"</automated>
  </verify>
  <acceptance_criteria>
    - docs/COVERAGE-MATRIX.md contains `## Processes` heading
    - docs/COVERAGE-MATRIX.md contains `## Network — TCP Connections` heading (em dash)
    - docs/COVERAGE-MATRIX.md contains `## Network — DNS Cache` heading (em dash)
    - Processes section contains fallback chain table with source values `cim`, `wmi`, `dotnet_fallback`
    - Processes section contains field fidelity table with all 17 field rows (ProcessId through Signature.SignerSubject)
    - Processes section contains dotnet_fallback supplement note mentioning ParentProcessId, CommandLine, SessionId, HandleCount as null
    - Processes section contains PS2 Signature limitation note mentioning X509Certificate.CreateFromSignedFile
    - Network TCP section contains source values `cim`, `netstat_fallback`, `netstat_legacy`
    - Network TCP section contains `CreationTime` documented as `null (all tiers)`
    - Network TCP section contains InterfaceAlias documented as `derived` at netstat tiers
    - Network TCP section contains netstat legacy known bug note (10% miss rate)
    - Network DNS section contains source values `cim`, `ipconfig_fallback`, `ipconfig_legacy`
    - Network DNS section contains Type field documented as integer (CIM) vs string (ipconfig)
    - Network DNS section contains TTL parsing fragility note
    - All method names, CIM class names, field names in backtick code spans
  </acceptance_criteria>
  <done>Processes, Network TCP, and Network DNS collector sections are complete with fallback chain tables, field fidelity tables, and coverage gaps. All data verified against RESEARCH.md pre-verified findings. Three of five collector sections done.</done>
</task>

<task type="auto">
  <name>Task 3: Write DLLs, Users, and Toolkit-Wide Coverage Gaps sections</name>
  <files>docs/COVERAGE-MATRIX.md</files>
  <read_first>
    - C:/DFIRLab/repo/docs/COVERAGE-MATRIX.md (current state from Tasks 1-2)
    - C:/DFIRLab/repo/Modules/Collectors/DLLs.psm1 (source of truth — .NET vs WMI enumeration, protected process list at two locations, Signature per tier)
    - C:/DFIRLab/repo/Modules/Collectors/Users.psm1 (source of truth — WMI+Registry scriptblock, DC detection, Get-ADUser/ADSI fallback)
    - C:/DFIRLab/repo/.planning/phases/01-coverage-matrix/01-RESEARCH.md (pre-verified field fidelity tables and fallback chains)
    - C:/DFIRLab/repo/.planning/phases/01-coverage-matrix/01-UI-SPEC.md (notation standards, copywriting contract, anchor stability)
    - C:/DFIRLab/repo/.planning/phases/01-coverage-matrix/01-CONTEXT.md (locked decisions D-05, D-06, D-12, D-13, D-16, D-17)
  </read_first>
  <action>
Append two collector sections and the toolkit-wide gaps section to docs/COVERAGE-MATRIX.md.

**## DLLs section:**

Per UI-SPEC copywriting template: `DLLs collects loaded module (DLL) details for each process from the target. The collection method and available data fields vary by tier.`

Fallback chain table (per D-05, verified from DLLs.psm1):

| Tier | Condition | Collection Method | `source` value |
|------|-----------|-------------------|----------------|
| PS 3+ | PSVersion >= 3 | `[System.Diagnostics.Process]::GetProcesses()` + `.Modules` | `dotnet` |
| PS 2.0 | PSVersion < 3 | `Win32_ProcessVMMapsFiles` WMI association, fallback to `CIM_ProcessExecutable` | `wmi` |

Note explicitly: These are entirely different enumeration mechanisms (per D-05) — the WMI path is NOT a degraded version of the .NET path.

Note the WMI association query method: PS 2.0 path tries `Win32_ProcessVMMapsFiles` first; if that throws, tries `CIM_ProcessExecutable`. Both use `Antecedent`/`Dependent` traversal. If both fail, emits stub entry with `reason = 'WMI_UNAVAILABLE'` and no module data.

Protected process exclusion (per D-13, verified from DLLs.psm1 — hardcoded at lines ~39 and ~172):
`lsass`, `csrss`, `smss`, `wininit`, `System`, `Idle` — these six processes are skipped at ALL tiers. Use the exact gap notation format from UI-SPEC:
`- **Protected processes skipped**: \`lsass\`, \`csrss\`, \`smss\`, \`wininit\`, \`System\`, \`Idle\` are never enumerated at any tier — hardcoded exclusion list in DLLs.psm1.`

Field fidelity table (per RESEARCH.md verified data, per D-12):

| Field | .NET `dotnet` (PS 3+) | WMI (PS 2.0) |
|-------|----------------------|--------------|
| `ProcessId` | populated | populated |
| `ProcessName` | populated | populated |
| `ModuleName` | populated (`.ModuleName`) | populated (filename from path) |
| `ModulePath` | populated (`.FileName`) | populated (from WMI association object) |
| `FileVersion` | populated (`[FileVersionInfo]::GetVersionInfo`) | populated (`CIM_DataFile.Version`) |
| `Company` | populated (`FileVersionInfo.CompanyName`) | populated (`CIM_DataFile.Manufacturer`) |
| `FileHash` (SHA256) | populated | populated |
| `IsPrivatePath` | populated (checks `\Temp\`, `\AppData\`, `\ProgramData\`, `\Users\Public\`, `\Downloads\`) | populated (same logic) |
| `Signature.IsValid` | populated (bool) | **null** |
| `Signature.Thumbprint` | populated | **null** |
| `Signature.NotAfter` | populated | **null** |
| `Signature.TimeStamper` | populated | **null** |
| `Signature.Status` | populated (e.g. `Valid`) | `PS2_SIGNED` or `PS2_UNSUPPORTED` |
| `Signature.SignerSubject` | populated | populated (X509 Subject only) |
| `LoadedAt` | null (all tiers) — neither API provides DLL load timestamp | null (all tiers) |

Note FileVersion/Company source difference: .NET path uses `System.Diagnostics.FileVersionInfo` (file metadata). WMI path uses `Get-WmiObject CIM_DataFile` (WMI database). Values may differ for files where the WMI database is stale or incomplete.

Coverage gaps for DLLs (per D-13):
- **Protected processes skipped**: `lsass`, `csrss`, `smss`, `wininit`, `System`, `Idle` — never enumerated at any tier (hardcoded exclusion list)
- **No DLL load timestamps**: `LoadedAt` is null at all tiers — no Windows API provides this
- **ACCESS_DENIED stubs**: Processes where `.Modules` access throws ACCESS_DENIED (.NET path) produce a stub entry with `reason='ACCESS_DENIED'` and no module data
- **PS 2.0 Signature limitation**: Same binary PS2_SIGNED/PS2_UNSUPPORTED limitation as Processes (see Processes section)
- **FileVersion/Company source divergence at PS 2.0**: WMI `CIM_DataFile` values may differ from .NET `FileVersionInfo` for files where the WMI database is stale

**## Users section:**

Per UI-SPEC copywriting template: `Users collects local account inventory, user profile metadata, and (on domain controllers) domain account details from the target. The collection method and available data fields vary by tier.`

Key architectural note per D-06: The entire `$script:USERS_SB` scriptblock runs at ALL PS versions without branching on PSVersion. It uses `Get-WmiObject` (not `Get-CimInstance`) throughout — PS 2.0-compatible and works identically on PS 2.0 through PS 5.1.

Collection sub-components table (per RESEARCH.md, verified from Users.psm1):

| Sub-component | Method | PS Version Dependency |
|---------------|--------|-----------------------|
| System info (is_dc, domain, machine name) | `Get-WmiObject Win32_ComputerSystem` | None — WMI at all tiers |
| Machine SID prefix | `Get-WmiObject Win32_UserAccount` | None — WMI at all tiers |
| User profiles | `HKLM:\...\ProfileList` registry enumeration | None — `Microsoft.Win32.Registry` API |
| Profile timestamps | ProfileFolder mtime + NTUSER.DAT mtime + Registry LastWriteTime | None — file system + registry |
| Local accounts | `Get-WmiObject Win32_UserAccount -Filter "LocalAccount=True"` | None — WMI at all tiers |
| DC domain accounts | `Get-ADUser` (preferred) -> ADSI `DirectorySearcher` (fallback) | None — PS version independent |

Source field construction (from Invoke-Collector):
- Non-DC: `source = 'WMI'`
- DC with Get-ADUser: `source = 'WMI+Get-ADUser'`
- DC with ADSI fallback: `source = 'WMI+ADSI'`

DC domain account fidelity table (per RESEARCH.md, per D-16):

| Field | `Get-ADUser` | ADSI (`DirectorySearcher`) |
|-------|-------------|---------------------------|
| `SamAccountName` | populated | populated |
| `DisplayName` | populated | populated |
| `SID` | populated | populated (byte array converted to `SecurityIdentifier`) |
| `Enabled` | populated | derived from `userAccountControl` flag (bit 2) |
| `WhenCreatedUTC` | populated | populated (`whencreated` attribute) |
| `LastLogonUTC` | populated (`LastLogonDate` — replicated) | populated (`lastlogontimestamp` — replicated, up to 14 days lag) |
| `PasswordLastSetUTC` | populated | populated (`pwdlastset` FILETIME) |
| `LockedOut` | populated | derived from `lockouttime > 0` |
| `BadLogonCount` | populated | populated (`badpwdcount`) |

Add ADSI replication lag note (per UI-SPEC copywriting, exact text):
`DC domain accounts: LastLogonUTC reflects lastlogontimestamp replication, which occurs every up to 14 days by default. This applies to both Get-ADUser and ADSI paths — neither provides real-time per-DC logon data.`

Note per CLAUDE.md: SID is the primary cross-host user correlation key. Mention this in the Users section.

Coverage gaps for Users (per D-16):
- **No PS-version degradation for local/profile collection**: Same fields at all tiers — no fallback path
- **DC LastLogon replication lag**: `LastLogonUTC` has up to 14-day replication lag regardless of `Get-ADUser` vs ADSI — Active Directory default
- **No session enumeration**: Collection is account inventory, not active session state — no logged-on users or active sessions

**## Toolkit-Wide Coverage Gaps section:**

Per UI-SPEC copywriting: `The following artifact types are never collected at any tier. These are explicit scope boundaries, not collector failures.`

Table (per D-17, verified from CLAUDE.md and RESEARCH.md):

| Gap | Notes |
|-----|-------|
| Windows Event Logs | Explicitly out of scope |
| Registry hives (raw) | Registry is read only for `ProfileList` key during Users collection |
| Memory dumps / RAM contents | Out of scope |
| File system artifacts (file listings, hash scans) | Out of scope |
| Network packet captures | Out of scope |
| Services / scheduled tasks | Not a collector module |
| Autorun / persistence locations | Not a collector module |

After writing all sections, verify the complete document has all 10 H2 headings per UI-SPEC anchor stability contract:
1. Legend
2. Tier Model
3. Capability Probe
4. Processes
5. Network — TCP Connections
6. Network — DNS Cache
7. DLLs
8. Users
9. Toolkit-Wide Coverage Gaps

(Note: H1 is the document title, not an H2)
  </action>
  <verify>
    <automated>pwsh -Command "$f = Get-Content C:/DFIRLab/repo/docs/COVERAGE-MATRIX.md -Raw; $headings = @('## Legend','## Tier Model','## Capability Probe','## Processes','## Network','## DLLs','## Users','## Toolkit-Wide Coverage Gaps'); $missing = $headings | Where-Object { $f -notmatch [regex]::Escape($_) }; if ($missing.Count -eq 0 -and $f -match 'lsass.*csrss.*smss.*wininit.*System.*Idle' -and $f -match 'WMI\+Get-ADUser' -and $f -match 'Windows Event Logs') { 'PASS' } else { 'FAIL: missing=' + ($missing -join ', ') }"</automated>
  </verify>
  <acceptance_criteria>
    - docs/COVERAGE-MATRIX.md contains `## DLLs` heading
    - docs/COVERAGE-MATRIX.md contains `## Users` heading
    - docs/COVERAGE-MATRIX.md contains `## Toolkit-Wide Coverage Gaps` heading
    - DLLs section contains fallback chain with source values `dotnet` and `wmi`
    - DLLs section contains protected process list: `lsass`, `csrss`, `smss`, `wininit`, `System`, `Idle` (exactly these six)
    - DLLs section contains `LoadedAt` documented as `null (all tiers)`
    - DLLs section contains note about Win32_ProcessVMMapsFiles and CIM_ProcessExecutable as two WMI association methods
    - DLLs section contains ACCESS_DENIED stub entry gap
    - DLLs section contains FileVersion/Company source divergence note
    - Users section documents NO PS-version branching for local/profile collection
    - Users section contains source values `WMI`, `WMI+Get-ADUser`, `WMI+ADSI`
    - Users section contains DC fidelity table with Get-ADUser vs ADSI columns
    - Users section contains ADSI replication lag note mentioning "up to 14 days"
    - Users section mentions SID as primary cross-host correlation key
    - Toolkit-Wide Coverage Gaps section lists: Windows Event Logs, Registry hives, Memory dumps, File system artifacts, Network packet captures, Services/scheduled tasks, Autorun/persistence
    - Complete document has all 9 H2 headings per UI-SPEC anchor stability contract
    - All method names, field names, source values in backtick code spans
  </acceptance_criteria>
  <done>All five collector sections (Processes, Network TCP, Network DNS, DLLs, Users) and Toolkit-Wide Coverage Gaps are complete. The document covers all four requirements: COV-01 (artifact grid), COV-02 (fallback paths), COV-03 (coverage gaps), COV-04 (fidelity notes). docs/COVERAGE-MATRIX.md is a complete, self-contained reference document ready for human verification.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. File exists and is non-empty: `Test-Path C:/DFIRLab/repo/docs/COVERAGE-MATRIX.md`
2. All 9 H2 headings present per UI-SPEC anchor stability contract
3. All five collectors documented: Processes, Network TCP, Network DNS, DLLs, Users
4. All four requirement areas covered: artifact grid (COV-01), fallback paths (COV-02), coverage gaps (COV-03), fidelity notes (COV-04)
5. All source field string values match authoritative values from RESEARCH.md: `cim`, `wmi`, `dotnet_fallback`, `cim+dotnet_fallback`, `wmi+dotnet_fallback`, `netstat_fallback`, `netstat_legacy`, `ipconfig_fallback`, `ipconfig_legacy`, `dotnet`, `WMI`, `WMI+Get-ADUser`, `WMI+ADSI`
6. Protected process list is exactly: lsass, csrss, smss, wininit, System, Idle
7. No deferred ideas (COV-05, COV-06, MITRE ATT&CK) appear in the document
</verification>

<success_criteria>
- docs/COVERAGE-MATRIX.md exists with 200+ lines
- Document renders as valid GitHub-flavored Markdown
- All 9 H2 heading anchors are stable per UI-SPEC contract
- Every collector has: fallback chain table, field fidelity table, coverage gaps list
- Every fidelity note names exact fields and their null/degraded state (not vague "degraded")
- Toolkit-Wide Coverage Gaps section lists all 7 out-of-scope artifact types
- No external URLs in the document (air-gap compliant per CLAUDE.md)
- All PowerShell/CIM/WMI class names, field names, and source values in backtick code spans
</success_criteria>

<output>
After completion, create `.planning/phases/01-coverage-matrix/01-01-SUMMARY.md`
</output>
